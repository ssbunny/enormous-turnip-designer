// TIP: handsontable 多级菜单存在 BUG，无法使用。
/*
export function formatItem() {
    return {
        name: '格式',
        disabled: function () {
            return !(this.getSelectedRange() && !this.selection.selectedHeader.corner);
        },
        submenu: {
            items: [{
                key: 'format:numeric',
                name: '数字 1,000.12',
                callback: function (sheet) {


                }
            }]
        }
    };
}
*/




